// Циклы

//===============================================================================================//

// Цикл while
/* 
    while (Условие выхода из цикла) {
        'тело цикла'
        код для каждой итерации
    } 
*/ 

// let i = 10
// let sum = 0

// while (i > 0) {
//     sum += i 
//     i--
// }

// console.log(`Result: ${sum}`)

// let result = 0

// while (true) {
//     const userInput = prompt('Your number')
//     if (!userInput) {
//         break // выход из цикла
//     }
//     result += Number(userInput)
// }

// console.log(`Result: ${result}`)

//===============================================================================================//

// Цикл do while
/* 
    do {
        // тело цикла
        // код выполнится хотя бы один раз
    } while (Условие выхода из цикла)   
       
*/ 

// let i = 10
// let sum = 0

// do {
//     sum += i 
//     i--
// } while (i > 0)

// console.log(`Result: ${sum}`)

// let m = 10
// const n = 20
// let result = 1

// do {
//     if (m % 2 === 1) {
//         result *= m
//     }
//     m++
// } while (m <= n)

// console.log(`Result: ${result}`)

//===============================================================================================//

// Цикл for
/* 
    for (инициализация; условие выхода из цикла; шаг) {
        'тело цикла'
    } 
*/ 

// let result = 1;
// for (let m = 10; m <= 20; m++) {
//     if (m % 2 === 1) {
//        result *= m;
//     }
// }
// console.log('Result: ' + result);

// for (let i = 1; i <= 10; i++) {
//     console.log(`\n////${i}////\n`)
//     for (let j = 1; j <= 10; j++) {
//         console.log(`${i} x ${j} = ${i * j}`)
//     }
// }

// let result = 1

// for (let m = 10; m <= 20; m++) {
//     if (m % 2 === 0) {
//         continue; // выход из цикла и переход к следующему
//     }
//     result *= m
// }

// console.log('Result: ' + result);

//===============================================================================================//

// Цикл for of (для перебора массива. не предоставляет доступа к номеру текущего элемента, только к его значению)
// function sum(numbers) {
//     let sumOfNumbers = 0
//     for(let num of numbers) {
//         sumOfNumbers += num
//     }
//     return sumOfNumbers
// }
// const numbers = [11, 22, 33, 55, 66]
// const sumOfElements = sum(numbers)
// console.log(`Result: ${sumOfElements}`)


// Цикл for in (работает для настоящих объектов, а не массивов)
// Цикл for..in выполняет перебор всех свойств объекта, а не только цифровых.
// Цикл for..in оптимизирован под произвольные объекты, не массивы, и поэтому в 10-100 раз медленнее.

















































































































// 📌 Циклы в JavaScript

//===============================================================================================//
// 🔹 Цикл while
// Используется, когда заранее не известно количество итераций.
// Выполняется до тех пор, пока условие истинно.

let i = 10;
let sum = 0;

while (i > 0) {
    sum += i; // Добавляем i к sum
    i--;      // Уменьшаем i
}
console.log(`Result: ${sum}`); // 55

// ❌ Бесконечный цикл
// while (true) {
//     console.log("Этот цикл будет выполняться вечно, если его не остановить!");
// }

// 📌 Прерывание цикла с помощью break
let result = 0;
while (true) {
    const userInput = prompt('Введите число:');
    if (!userInput) break;  // Если ввод пустой, выход из цикла
    result += Number(userInput);
}
console.log(`Result: ${result}`);

//===============================================================================================//
// 🔹 Цикл do while
// Отличается от while тем, что код внутри **выполнится хотя бы один раз**,
// даже если условие окажется ложным.

let j = 10;
let sumDoWhile = 0;

do {
    sumDoWhile += j;
    j--;
} while (j > 0);

console.log(`Result: ${sumDoWhile}`); // 55

// 📌 Пример с условиями
let m = 10;
const n = 20;
let product = 1;

do {
    if (m % 2 === 1) {
        product *= m;  // Умножаем только нечетные числа
    }
    m++;
} while (m <= n);

console.log(`Result: ${product}`); // 111546435

//===============================================================================================//
// 🔹 Цикл for
// Используется, когда количество итераций известно заранее.

for (let i = 1; i <= 10; i++) {
    console.log(`\n////${i}////\n`);
    for (let j = 1; j <= 10; j++) {
        console.log(`${i} x ${j} = ${i * j}`);
    }
}

// 📌 Оператор continue (пропуск итерации)
for (let m = 10; m <= 20; m++) {
    if (m % 2 === 0) {
        continue; // Пропускаем четные числа
    }
    console.log(m); // Выводим только нечетные числа
}

//===============================================================================================//
// 🔹 Цикл for..of (перебор массива)
// Позволяет перебирать элементы массива **без доступа к индексам**.

const numbers = [11, 22, 33, 55, 66];

for (let num of numbers) {
    console.log(`Элемент: ${num}`);
}

// 📌 Функция для суммирования элементов массива
function sum(numbers) {
    let sumOfNumbers = 0;
    for (let num of numbers) {
        sumOfNumbers += num;
    }
    return sumOfNumbers;
}
console.log(`Result: ${sum(numbers)}`); // 187

//===============================================================================================//
// 🔹 Цикл for..in (перебор свойств объекта)
// Предназначен для перебора **свойств объектов**, а не массивов.

const user = {
    name: "Alice",
    age: 25,
    city: "Paris"
};

for (let key in user) {
    console.log(`${key}: ${user[key]}`);
}
// name: Alice
// age: 25
// city: Paris

// ❌ Использование for..in для массивов (не рекомендуется)
const arr = [10, 20, 30];
for (let index in arr) {
    console.log(arr[index]); // Не гарантирует правильный порядок
}

//===============================================================================================//
// 🎯 Итог:
// ✅ Цикл while - используется, когда неизвестно количество итераций.
// ✅ Цикл do while - выполняет тело цикла хотя бы один раз.
// ✅ Цикл for - используется, когда количество итераций известно.
// ✅ Цикл for..of - удобен для перебора массива (без индексов).
// ✅ Цикл for..in - используется для перебора свойств объекта.

